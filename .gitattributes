*.psd filter=lfs diff=lfs merge=lfs -text
# *.pdf removed from LFS tracking (May 2026) — the org's LFS bandwidth
# quota was hit and every Vercel clone was failing. Small PDFs like
# /public/mudigere-estate-map.pdf are fine as regular git blobs. If
# someone wants to LFS-track only oversized PDFs, use a narrower glob
# (e.g. Info/large/*.pdf) rather than the catch-all *.pdf.
